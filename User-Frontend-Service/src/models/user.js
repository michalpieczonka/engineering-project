export default class User {
    constructor(username, password, email, token, id) {
        this.username = username;
        this.password = password;
        this.email = email;
        this.token = token;
        this.id = id;
    }
}